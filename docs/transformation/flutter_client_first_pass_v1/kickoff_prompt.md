# Flutter Client First Pass V1 Executor Kickoff

Repository:

`C:\Users\artwh\OneDrive\Documents\legal2`

Implement the complete authoritative packet:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\flutter_client_first_pass_v1\README.md`

You are executing from clean context. Do not rely on prior conversation.

Before editing:

1. Read repository `AGENTS.md` completely.
2. Read every packet file completely in the exact README order.
3. Execute FCFP1-000 and record the dirty-worktree baseline.
4. Read every current Flutter source/test file and the exact Python/server
   contract references named by the packet.
5. Preserve unrelated user work. Do not reset, clean, revert, checkout,
   commit, push, deploy, or create a PR.

Execute FCFP1-000 through FCFP1-900 in dependency order. Complete each
ticket's implementation, focused tests, regression, residue checks, and
execution-log entry before starting dependent work.

Maintain:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\flutter_client_first_pass_v1\execution_log.md`

Binding outcome:

- one Flutter process owns one v15 EVW and one shared evidence controller;
- five top-level pages exist in order: Corpus, Search, Conversation,
  Transcript, Print output;
- Corpus performs real open, close, and explicit working-corpus selection,
  resolving only its current ready revision;
- Search provides scoped local FTS5 and server-generated query
  embedding/local vector search, a scrollable result list, result navigation,
  evidence save, and the reusable transcript editor;
- keyword expansion is absent from Flutter UI/workflows but untouched on the
  server;
- Conversation ports the current exact planner/semantic retrieval/analysis
  stream boundary to typed Dart, visibly reports progress/cancel/failure,
  preserves every returned result class, makes verified ranges navigable and
  saveable, and shows the reusable transcript editor;
- conversational range evidence preserves the complete relevant range plus up
  to three preceding/following same-thread context messages;
- Transcript is the full existing virtual evidence editor;
- Print output is a limited functional artifact organizer and native document
  preview using existing tables, with no fake Print/PDF/export control;
- transcript scroll positions are independent, evidence data is shared, and
  only the visible page can drive center-line active evidence;
- the server remains stateless/EVW-blind and unchanged;
- no import/filter/corpus-builder/auth/billing/BYOK/settings/dashboard/mock
  controls are invented;
- no EVW schema or migration changes are made;
- no Python or server source is changed.

Use the existing Flutter dependencies and `dart:io`; do not introduce routing,
state-management, ORM, code-generation, or design-system frameworks.

Routine and final validation must make zero external model-provider calls and
must not rebuild a real corpus's embeddings. Use deterministic fake HTTP
JSON/NDJSON servers and disposable copies of real v15 EVWs. Run formatting,
analysis, all Flutter tests, real-EVW gates, fake-server gates, Windows release
build, residue scans, and cleanup yourself.

At completion create:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\flutter_client_first_pass_v1\closeout_report.md`

Include ticket/gate results, commands and totals, changed files, final
ownership and operation maps, shared-evidence proof, zero-provider-call proof,
real-EVW copy cleanup, factual limitations, and lean human manual-test
instructions.

Continue autonomously until complete or a genuine stop condition in
`08_executor_protocol.md`. Begin with FCFP1-000 now.

