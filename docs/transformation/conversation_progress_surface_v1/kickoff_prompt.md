# Conversation Progress Surface V1 - Executor kickoff

Repository:

`C:\Users\artwh\OneDrive\Documents\legal2`

Implement the complete authoritative packet:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\conversation_progress_surface_v1\README.md`

You are executing from clean context. Do not rely on prior conversation.

Before editing:

1. Read repository `AGENTS.md` completely.
2. Read every packet file completely in the exact README order.
3. Execute CSP1-000 and record the complete dirty-worktree baseline.
4. Read every current source and test file named by the packet.
5. Preserve unrelated work. Do not reset, clean, revert, checkout, commit,
   push, deploy, or create a PR.

Execute CSP1-000 through CSP1-900 in dependency order. Complete each ticket's
implementation, focused tests, diff inspection, residue checks, and
execution-log entry before starting dependent work.

Maintain:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\conversation_progress_surface_v1\execution_log.md`

Binding outcome:

- conversational progress is truthful and derived only from real client
  actions and existing server events;
- one narrow server event extension exposes every already-validated accepted
  range at window completion;
- no provider request, prompt, model choice, orchestration path, retry policy,
  or usage accounting changes;
- Flutter displays a live elapsed timer, exact stage, exact window progress,
  active windows, retries, warnings, provisional evidence, and full activity;
- provisional evidence is read-only, uncapped, and explicitly nonfinal;
- successful final synthesis replaces provisional presentation;
- failure and cancellation preserve incomplete activity visibly;
- conversation cards and active progress survive every top-level tab change;
- timers continue while Conversation is offstage and freeze on terminal state;
- cards remain for the app session while the same revision is selected;
- revision changes still clear cards; app restart does not restore progress;
- no progress or provisional data is persisted to the EVW;
- existing completed-conversation persistence remains unchanged;
- Python GUI/workflow, EVW schema, search, transcript, print, admin, auth,
  billing, and BYOK remain untouched.

The temporary Python API-contract mirror may be updated only for the exact
extended event. Do not develop the Python client.

Use existing dependencies. Do not introduce state-management, routing,
code-generation, ORM, analytics, or design-system frameworks.

All automated validation must make zero external provider calls and must not
rebuild real embeddings or mutate a real EVW. Run formatting, analysis, tests,
Windows release build, residue scans, and cleanup yourself.

At completion create:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\conversation_progress_surface_v1\closeout_report.md`

Continue autonomously until complete or a genuine stop condition in
`05_executor_protocol.md`. Begin with CSP1-000 now.
