# Result-Preserving Conversational Validation V1 Executor Kickoff

Repository:

`C:\Users\artwh\OneDrive\Documents\legal2`

Implement the complete authoritative packet:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\result_preserving_validation_v1\README.md`

You are executing from clean context. Do not rely on prior conversation.

Before editing:

1. Read repository `AGENTS.md` completely.
2. Read every packet document completely in exact README order.
3. Inspect and record the dirty worktree/baseline required by RPV1-000.
4. Treat this packet as authoritative where it conflicts with older
   synthesis, disposition, finding-validation, terminal-failure, or result
   contracts.
5. Preserve unrelated user work. Do not reset, clean, revert, checkout, commit,
   push, deploy, or create a PR.

Execute RPV1-000 through RPV1-900 in dependency order. Complete each ticket's
implementation, required deletion, focused tests, relevant regression, residue
scan, and execution-log entry before beginning dependent work.

Maintain:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\result_preserving_validation_v1\execution_log.md`

The binding product goal is:

> Return every intelligible result. Overcollection is preferable to omission.
> Validation verifies real source identity and annotates limitations; it never
> discards a readable answer.

Required end state:

- Remove `direct_evidence`, `useful_context`, `not_responsive`,
  `range_dispositions`, direct-only finding rules,
  `partial_evidence_validation`, and `LEDGER_BIJECTION_FAILED` from every
  active runtime/client/script/current-test path.
- Synthesis returns one result list. Every model result contains its own
  `high_probability|lower_probability` responsiveness category and exact range
  citations. There is no parallel disposition list.
- The server presents high-probability results first, then a visible lower-
  probability section. It never hides lower-probability results.
- Every canonical extraction range omitted by synthesis remains visible as
  unclassified evidence after lower-probability results.
- A readable final-model response always reaches the client, unchanged.
  Malformed/nonconforming JSON becomes a returned raw synthesis answer with
  warnings, not a failed request.
- Every result citation is checked independently. Mixed valid/fabricated
  citations preserve the statement and valid links. Unknown IDs are isolated
  and can never enter verified evidence or client navigation.
- Model statements with no verified source remain visibly separated as
  unverified model output; they are never represented as corpus-backed
  evidence.
- Actual evidence text is loaded by the client from the EVW using verified
  IDs, never accepted as canonical model-generated quotation text.
- Only deterministic one-interpretation corrections are permitted: exact
  endpoint reversal, authoritative thread derivation, exact citation
  deduplication/order, and narrowly specified JSON-fence/object normalization.
  Never guess, shorten, fuzzily match, or replace opaque IDs.
- Valid sibling ranges survive malformed/fabricated ranges. Parseable
  all-invalid extraction output remains a usable partial window.
- A machine-unusable window retries only that window. Exhausted window failure
  does not cancel or erase completed siblings; synthesis continues with
  explicit partial coverage.
- Empty/absent synthesis retries only synthesis. Readable nonconforming
  synthesis is returned without automatic rerun.
- If synthesis remains unavailable, return the complete validated ledger and
  unclassified candidates as a `partial` completed result.
- Ledger compaction remains loud. If it fails, preserve the original ledger.
  If synthesis cannot fit afterward, return a partial ledger-only result.
- Terminal statuses are `complete|complete_with_warnings|partial`. Both warning
  and partial outcomes use the terminal `completed` stream event. `failed` is
  reserved for no useful answer/evidence or an unexecutable request.
- Add exact synthesis-received, validation, window-unavailable, warning, and
  accounting visibility. No silent retry/fallback/default/truncation.
- Remove the arbitrary 20,000-character synthesis-answer cap.
- Update the existing server admin with plain-language high/lower/source-
  verification explanations, exact schemas, and real content-free metrics.
  The return-everything policy is fixed, not configurable.
- Update the stripped Python client only as server test equipment: strict new
  contract, high/lower divider, unclassified/unverified sections, warnings
  without failure popups, verified-ID-only navigation, correct progress/timer,
  and visible-result persistence only.
- Keep exactly the existing four product POST routes and five model
  operations. Preserve frozen planning, exhaustive windows, local semantic
  retrieval, stateless EVW-blind server boundary, accounting, cancellation,
  debug capture, and next-request configuration activation.
- Do not modify Flutter, EVW schema/lifecycle, embedding persistence, corpus
  revision architecture, auth, billing, or BYOK.
- Do not revive deleted Python provider/search modules or create a second
  conversational path.

Use only the repository environment:

`C:\Users\artwh\OneDrive\Documents\legal2\.venv\Scripts\python.exe`

Install/repair repository-environment dependencies and run all automated work
yourself. Routine tests use deterministic fakes and make no real provider calls
or large embedding rebuilds.

After all local gates pass, execute the one authorized live validation in
`10_live_validation.md` using already configured GLM 5.2 credentials and the
exact prompt:

`Show me fights about school.`

Do not automatically repeat the complete expensive run, switch models, tune
after seeing output, or expose secrets. Existing configured operation-local
retries may occur and must be reported. Preserve the actual returned answer,
high/lower/unclassified/unverified sections, complete ledger metadata,
warnings, usage, timing, and artifact paths. Stop/flush debug capture, restore
temporary config, stop processes, and rerun deterministic gates.

If credentials are unavailable or the configured provider remains externally
unavailable after configured attempts, complete every local gate and record the
live gate as the sole external blocker. Never silently select another model.

At completion create:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\result_preserving_validation_v1\closeout_report.md`

Include every ticket/gate, exact commands/results, changed/deleted files,
prompt/config migration, final route/operation/result/event/error inventory,
source-integrity and result-preservation proofs, live answer/artifacts or exact
blocker, debug/config/process cleanup, no-write EVW/WAL evidence, test totals,
factual remaining risks, and lean human manual-test instructions.

Continue autonomously until completion or a genuine stop condition in
`11_executor_protocol.md`. Begin with RPV1-000 now.

