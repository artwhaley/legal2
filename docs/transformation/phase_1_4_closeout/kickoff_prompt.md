# Phase 1–4 Closeout Kickoff

Work in:

`C:\Users\artwh\OneDrive\Documents\legal2`

This is an implementation task, not a planning task. Read and obey `AGENTS.md`,
then read every file in:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\phase_1_4_closeout`

Start with `README.md` and follow its reading order. Execute every ticket in
`04_ticket_stack.md` from CLS-000 through CLS-402. The closeout packet
supersedes the old Phase 1–4 implementation where they conflict.

This is a deliberate clean break. Implement EVW v14, API v2, mandatory working-
corpus scope, one writer, remote-only model/embedding execution, and the lean
Flutter v14 viewer. Remove old schemas from runtime, dataset-only search APIs,
client provider/model/prompt code, local embedding execution, optional gateway
branches, hidden settings controls, old planner/synthesis, legacy window merge,
and compatibility shims. Rewrite or delete tests that specify deleted
architecture; do not preserve it to keep them green.

Never edit the user's only EVW. Use compact-copy migration and copied fixtures.
Preserve unrelated user changes. Do not use destructive git reset/checkout.
Do not add auth, billing, BYOK, cloud persistence, speculative Flutter screens,
silent retry, fallback, truncation, or hidden behavior.

Maintain:

`C:\Users\artwh\OneDrive\Documents\legal2\docs\transformation\phase_1_4_closeout\closeout_execution_log.md`

Work autonomously until every closeout gate passes or a stop condition in
`06_executor_protocol.md` occurs. Finish with `closeout_report.md` containing
the exact evidence required by `05_acceptance_and_closeout.md`.
